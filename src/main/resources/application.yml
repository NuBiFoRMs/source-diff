spring:
  devtools:
    add-properties: false

  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

source-diff:
  show-line-count: 30
  repositories:
    - name:
      dev-url:
      dev-username:
      dev-password:
      prod-url:
      prod-username:
      prod-password:

file-filter:
  prefix:
  postfix:
  contains: